h1 Recent messages:
md-content.md-padding(style="height:15em")
	ul
		li(ng-repeat="msg in messages track by $index") {{ msg }}

md-divider

form
	md-input-container(flex)
		label Your Message:
		input(ng-model="message")
	md-button.md-raised.md-primary(ng-click="sendMessage()") Send message
